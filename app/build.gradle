apply plugin: 'com.android.application'
//将app module共用的配置放在一个文件管理
apply from:rootProject.file('app.gradle')
//设置applicationId
apply from:rootProject.file('applicationId.gradle')
//将library module共用的配置放在一个文件管理
apply from:rootProject.file('libbase.gradle')