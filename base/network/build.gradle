apply plugin: 'com.android.library'
//将APP共用的配置放在一个文件管理
apply from: rootProject.file('libbase.gradle')

dependencies {
    //加载retrofit2库
    rootProject.ext.retrofit2.each {
        k, v -> implementation v
    }
}