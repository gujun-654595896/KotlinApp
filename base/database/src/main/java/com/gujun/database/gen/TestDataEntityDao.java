package com.gujun.database.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.gujun.database.entity.TestDataEntity;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "TEST_DATA_ENTITY".
*/
public class TestDataEntityDao extends AbstractDao<TestDataEntity, Long> {

    public static final String TABLENAME = "TEST_DATA_ENTITY";

    /**
     * Properties of entity TestDataEntity.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id_ = new Property(0, Long.class, "id_", true, "_id");
        public final static Property Name = new Property(1, String.class, "name", false, "NAME");
    }


    public TestDataEntityDao(DaoConfig config) {
        super(config);
    }
    
    public TestDataEntityDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"TEST_DATA_ENTITY\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id_
                "\"NAME\" TEXT);"); // 1: name
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"TEST_DATA_ENTITY\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, TestDataEntity entity) {
        stmt.clearBindings();
 
        Long id_ = entity.getId_();
        if (id_ != null) {
            stmt.bindLong(1, id_);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(2, name);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, TestDataEntity entity) {
        stmt.clearBindings();
 
        Long id_ = entity.getId_();
        if (id_ != null) {
            stmt.bindLong(1, id_);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(2, name);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public TestDataEntity readEntity(Cursor cursor, int offset) {
        TestDataEntity entity = new TestDataEntity( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id_
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1) // name
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, TestDataEntity entity, int offset) {
        entity.setId_(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setName(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(TestDataEntity entity, long rowId) {
        entity.setId_(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(TestDataEntity entity) {
        if(entity != null) {
            return entity.getId_();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(TestDataEntity entity) {
        return entity.getId_() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
